- name: Instant clone in check mode
  #hosts: localhost
  #vars:
  #    datastore: DATASTORE03
  community.vmware.vmware_guest_instant_clone:
    hostname: "{{ 10.2.250.110 }}"
    username: "{{ labtest1@vsphere.local }}"
    password: "{{ Labtest@@2022 }}"
    validate_certs: False
    folder: "{{ LABVM }}"
    datastore: "{{ vmdatastore }}"
    datacenter: "{{ HPE-LAB-DC }}"
    host: "{{ hplab-esxi04.hplab.local }}"
    name: "{{ pocvm }}"
    parent_vm: "{{ testvm_2 }}"
    resource_pool: "{{ test_resource_001 }}"
  check_mode: check_mode_clone
  register: vm_clone
  #delegate_to: localhost
  - debug:
      var: check_mode_clone
