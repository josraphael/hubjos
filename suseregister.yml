---
 - name: suse subscription and repo management
   hosts: all
   tasks:
   - name: activation
     shell: SUSEConnect -r 0E82D285B1F77651 -e jotiram9mane@gmail.com
   
   - name: activation status
     shell: SUSEConnect -s
     register: update_list
   - debug:
        var: update_list
        #verbosity: 2

   #- debug:
   #     msg: "{{ update_list.results | map(attribute='registered')}}"
  

